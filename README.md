# SOC-PINN
SOC-PINN Code
## Environment

- Python >= 3.8
- PyTorch >= 1.12.0
- NumPy >= 1.21.0
- Pandas >= 1.3.0
- scikit-learn >= 1.0.0
- Matplotlib >= 3.5.0
- tqdm >= 4.62.0
- openpyxl >= 3.0.0

GPU is optional. The code will use CUDA automatically if available, otherwise it runs on CPU.

## Install

```bash
pip install torch numpy pandas scikit-learn matplotlib tqdm openpyxl
